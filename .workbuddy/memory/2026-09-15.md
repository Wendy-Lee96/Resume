# 2026-09-15

## 简历作品集网站迭代
- 新增头像：裁剪 `Transform_this_photo_into_a_mi_2026-09-15T01-22-49.png` 为 600x600 正方形，保存为 `public/avatar.jpg`，并在 Hero 区右侧展示（圆角 + 描边 + 阴影）。
- 新增「工作经历」时间线板块：组件 `src/components/Experience.tsx`，展示法本/平安/锦绣前程/大秦人力 4 段经历，含公司、职位、时间、地点、职责亮点。
- 更新 `src/content.ts`：新增 `profile.avatarUrl`、`experiences` 数组，并在 `nav` 中加入「工作经历」。
- 更新 `src/App.tsx`：引入 `Experience` 组件并放在「关于我」之后。
- 验证：`npm run build` 通过，dev server 在 http://localhost:5173/ 正常运行。

## 二次微调（头像 + 站名）
- 头像由圆形 `rounded-full` 改为圆角四边形（`aspect-[4/5]` + `rounded-3xl` 竖版卡片，更贴合原竖图）。
- 新增 `profile.siteName`，导航栏 logo 与 `<title>` 改为该品牌名。用户确认 `Wendy.rusume` 为 `Wendy.resume` 笔误 → 已修正为 `Wendy.resume`。
- 构建时 vite 清空 dist 触发 WorkBuddy safe-delete/trash 失败 → 手动 `rm -rf dist` 后再 build 成功。

## 三次迭代（工作经历修正 + 技能云图）
- 工作经历按简历重新梳理为 4 家公司（时间倒序）：
  1. 深圳市法本信息技术股份有限公司（2022.05-至今，含中海全部经历）
  2. 广西锦绣前程人力资源有限公司（2019.03-2021.11）
  3. 深圳安络科技有限公司（2018.03-2019.03）
  4. 西安美齐电子商务有限公司（2016.07-2018.03）
- 移除 Hero 区头像，新增 `SkillCloud` 组件： skillGroups 标签以云状分布、持续缓慢漂浮、鼠标移入放大 1.25 倍。
- 在 `src/index.css` 增加 `cloud-float` 关键帧动画支持。
- `npm run build` 通过，dev server 在 http://localhost:5173/ 正常运行。

## 四次迭代（项目案例改为点击切换布局）
- `Projects.tsx` 重写为「左侧项目列表 + 右侧详情面板」结构（参考用户提供的 BIM 评审系统截图）。
- 交互补充：切换详情淡入动画（`animate-detail`，key 触发 remount）、成果数字卡片级联延迟入场、键盘 ↑↓/←→ 切换（focus 列表后可用）、底部「上一个/下一个」按钮 + 圆点分页指示器、右上角 0X/08 计数、每个项目专属彩色图标与选中态左侧指示条、移动端列表变横向滚动 chips。
- 坑：Tailwind 动态拼接 `grid-cols-${n}` 不生效 → 必须写全量类名三元分支。
- `npm run build` 通过。

## 五次迭代（插图 + 等高 + 产品思考重设计）
- 新增 `CubeIllustration.tsx`：纯 SVG 等距立方体插图（渐变面 + 网格线 + 聊天气泡/目标环/柱状图/人物浮块 + 散点，CSS 缓浮动画），`full`/`compact` 两档变体，深浅主题通用。
- 项目案例：标题右侧加 full 插图（lg+ 显示）；左右卡片等高（grid `items-stretch` + 左列表 `lg:min-h-0 lg:overflow-y-auto`，去掉 max-h-640）。
- 产品思考参考用户截图重做：`Product Thinking` 徽章 + 「我的产品思考。」标题 + 右侧插图；6 步卡片改为「渐变 tint 背景 + 彩色大序号 + 渐变图标块 + pill 标签 + 卡间 ChevronRight 衔接箭头（仅 lg）+ hover 上浮」；底部价值观横条（核心目标大格 + 4 小项）。
- 坑：lucide-react 无 `Sitemap` 图标 → 用 `Network`。
- `npm run build` 通过。

## 六次迭代（全站卡片风格统一）
- 新增 `src/palette.ts`：共享 8 色调色板 + `pick/cardBg/iconBg/chipBg/cardClass` 工具函数，是全站卡片风格的唯一来源。
- 已按「产品思考」风格重做的卡片：Hero 数据墙（彩色数字 + 底部色条）、About 优势卡（图标块 + 渐变底 + hover 上浮）、Experience 时间线条目（改为渐变卡片 + 彩色时间 pill + 彩色轴点）、Skills 技能组（彩色 chip）、Projects（列表图标/序号/成果数字卡/概述块/tags/功能 pill 全部按色板着色）、Toolbox（图标块 + 渐变 + chip）、Contact（复制行 + 联系点卡片）、Thinking（改引用共享 palette）。
- 插图：用户反馈现有 SVG 立方体插图不够好，将自行提供素材 → 后续替换 `CubeIllustration`（用于项目案例与产品思考两处标题右侧）。
- 坑：未使用的 import 在 `tsc --noEmit` 下报 TS6133，需清理。
- `npm run build` 通过。

## 七次迭代（回退与布局调整）
- 工作经历回退为非卡片式：恢复 md 左右交替时间线（左列时间+公司，右列职责亮点列表，accent 轴点），不再用渐变卡片。
- 关于我改为上下结构：文字段落在上，4 张优势卡移到下方一行（sm:2 列 / lg:4 列），取消左右分栏。
- 项目案例与产品思考的 CubeIllustration 插图全部撤掉（标题区恢复为纯文字块）。组件文件保留，等用户给素材后再启用。
- `npm run build` 通过。

## 八次迭代（用户素材插图接入）
- 用户提供 3 张 3D 插图素材（已复制到 `public/images/`）：
  - `ai-3d.jpg`（AI 蓝色 3D）→ 产品思考标题右侧
  - `data-cubes.jpg`（数据可视化立方体）→ 项目案例标题右侧
  - `office-scene.jpg`（办公桌场景）→ 联系我板块背景图
- 插图用 `<img>` 圆角直出，浅色模式原样、深色模式 `dark:opacity-25` 防白底突兀。
- 联系我背景：`opacity-[0.08]`（深色 0.06）+ 左向渐变遮罩保证文字可读。
- 素材来源：摄图网（非企业商用授权）+ AI 生成。
- `npm run build` 通过，图片 HTTP 200。

## 九次迭代（插图位置再调整）
- `data-cubes.jpg` 从项目案例标题右侧移到「我的能力地图」（专业技能）板块底部靠左，宽度 max-w-sm/sm:max-w-md + 圆角。
- 项目案例、产品思考标题区插图全部撤除，恢复纯文字标题块。
- 首屏向下箭头改为毛玻璃圆形按钮（backdrop-blur-md + 半透明底/白描边 + 阴影 + hover 变色）；顺带修复 `-translate-x-1/2` 与 `animate-bounce` 同元素冲突导致位置偏右（拆成外层居中容器 + 内层弹跳）。
- `npm run build` 通过。

## 十次迭代（章节 pill 标签 + 专业技能左右布局）
- 新增 `src/components/Eyebrow.tsx`：圆弧浅底 pill 小标签组件（accent/10 底 + accent 文字），全站章节统一复用：关于我 / 工作经历 / 专业技能 / 项目案例 / AI Toolbox / 联系我 / Product Thinking。
- 专业技能改为左右结构：`lg:grid-cols-[0.8fr_1.2fr] items-stretch`，左侧插图（`h-full object-cover` 高度自适应右侧两卡叠加高度，移动端 min-h-240），右侧两张能力卡纵向堆叠（`lg:grid-rows-2`）。
- 插图边缘虚化：`mask-image` 径向渐变（ellipse 84% 80%，实心到 60%），并去掉容器 border。
- `npm run build` 通过。

## 十一次迭代（文案修订 + 关于我重构 + 高度/背景修正）
- Hero 主标题改为「用 AI 赋能业务落地，/ 用产品思维解决真实价值」。
- `profile.title` 中「高级产品经理」→「资深产品经理」（仅该串；工作经历法本条目职称仍为「高级产品经理」，如需同步再改）。
- 关于我：总结段替换为用户新文案；`content.ts` 新增 `about.bullets` 四要点。
- 关于我布局：上总结段 → 下左侧 4 个彩色 bullet + 右侧 4 张卡片 2×2（`lg:grid-cols-[1fr_1.15fr] items-start`）。
- 联系我背景图改为居中铺满（`inset-0 object-cover object-center` + 上下渐隐遮罩），原先靠右 62% 显歪。
- 项目案例高度：右侧详情按内容自适应；左侧改「外层占位 + 内层 `lg:absolute lg:inset-0 lg:overflow-y-auto`」跟随右侧高度并可纵向滚动（`lg:min-h-[460px]` 兜底）。
- `npm run build` 通过。

## 十二次迭代（删除关于我优势卡 + 文案微调）
- Hero 主标题定稿：「用 AI 赋能业务落地 / 用产品思维创造真实价值」（无逗号，"创造"）。
- bullets：第 2 条 →「3 年团队管理经验，组建产品中心，规范需求、项目与迭代流程。」；第 3 条 →「从企业级后台到 IoT 控制的跨行业需求理解」；第 4 条 →「…串成闭环 有实际落地经验」。修过一次重复行 bug。
- 按用户红框截图，删除「关于我」的 4 张总结卡片，板块简化为：标题 + 总结段 + 4 个 bullet（sm:2 列网格铺开）。
- `content.ts` 的 `about.advantages` 数据保留（组件不再引用），便于恢复。
- 关于我改为左右结构：新增用户提供的动漫插画风「AI PM 全链路 AI 产品人」图（`public/images/ai-pm.jpg`）放左侧，右侧总结段 + 4 bullet（sm:2 列）；`lg:grid-cols-[0.85fr_1.15fr] items-stretch`，图片 `h-full object-cover` 自适应右侧高度，DOM 顺序内容在前 + `lg:order-first` 使移动端先读文字。
- 关于我左侧改为「个人名片卡」（参考用户截图）：竖版新插画（AI产品人动漫插画风设计方案.jpeg，覆盖 ai-pm.jpg）+ 名字「李文文 Wendy」+ 职称 + 地点/邮箱/电话（lucide 图标行）+「查看我的简历」accent 链接；卡片 `h-full flex flex-col`，图片区 `flex-1 min-h-[220px]` 自适应右侧高度。
- `npm run build` 通过。
